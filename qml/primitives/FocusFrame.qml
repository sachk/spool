import QtQuick
import "../theme"

Rectangle {
    color: "transparent"
    radius: Theme.radiusMedium
    border.width: Theme.focusBorderWidth
    border.color: Theme.accent
    visible: false
    antialiasing: true
}

- [x] fix the audio video sync settings being set to the webos ones for non-webos playforms. we should be using mpv's defaults there - so video-sync=audio generally. currently it takes a while for the sync to happen on desktops, on webos we're doing weird shit to sync starfish to alsa and we really don't want any of that stuff leaking over but i want to keep the mpv branch the same for now.
- [x] add better subtitle settings. on webos, we need a way to know if the display is in a hdr mode and automatically make the subtitles not bright white, but "paper white". think about how to handle this and the options surrounding it nicely. for subtitles we should have an option to override fonts, a master option to make all subtitles even bitmap ones smaller/larger to a certain by a smart font-size and for bitmap bitmap size scaling algorithm. of course also options for where to put it. from my friend: "i think having more subtitle customization settings would be really nice
  usually i have subtitles on because i hate missing dialogue but i make the font super small so i can ignore it unless i need to
  i think a few fonts to choose from, colours, size and vertical position (cuz i position them differently on 16:9 versus 4:3)
  this is something that the native client doesn't have
  "

- [x] we should also do subtitle delay obviously, that needs to be per file and not remembered like audio delay is and under the hamburger menu in the player. we should probably split audio delay into per file and global and have a nicely done intuitive system for changing which ur modifying and then they're combined. think that one through.

- [x] on the choose a zoom setting page remove all the idiotic editoralising - it's for screens that are different distances/sizes away there is no "BEST SIZE" and the subtext and "make jellyfin yours" is stupid. It should just say "Choose a comfortable zoom" with no retarded subtitle and then the examples for each level should be redesigned to be more obvious. and they should just have a name. no "reccomended".

- [x] On LG WebOS the backspace key must work for back as it's what the remote sends for the back arrow even within text field. However, this breaks backspacing in the password/username fields on the login screen etc on desktops. You should make backspace not work as a back button on non-lg webos platforms when typing in a text field. Do this properly - it has to work everywhere on every platform and be no messier than it is now.

- [x] There should be breif instructions under jellyfin syncplay's number that say "Login from another device" to make it a bit less confusing. These should be reasonably sized.

- [x] The UI zoom option selection thing should show up immediately on first launch before the server select screen.

- [x] The UI zoom range in settings should be increased a bit. It should also be the first option in the settings menu.

- [x] Servers that are typed into and connected to one should be remembered in the list for servers to select to then try logging into even if they are not yet logged into/the app is closed before they're logged into.

- [x] Mark as watched/unwatched icon should be changed to be more obvious.

- [x] New episodes added for the same show and season should be grouped together. It should show a range like E01-E13 or if they're fragmented just the newest ep. Right now there can be ten cards for episodes from the same new season of one show - BAD

- [x] You should include a good tasteful oss serif font for subtitles and an option that only works on desktop platforms to choose a system font from a list of all system fonts using whatever best way to implement such a thing with Qt, whether that's opening a system dialogue if possible or something else.

- [ ] Add desktop keyboard/key remapping. The existing color-button remapping is for TV remotes and should stay hidden on desktop builds.

- [x] Move the settings cog, audio-track, subtitle-track, and related player controls to the right side of the screen. Keep the playback controls on the left with flexible empty space between the two groups, matching the common player layout so the controls are quicker to understand.

- [x] Increase the maximum UI zoom so the largest setting is more zoomed in.
